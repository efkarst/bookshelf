<div class="grid-item" >
  <%= link_to image_tag(book.cover_image), book_path(book) %>
  <p class='label'><%= link_to book.title, book_path(book) %></p>
  <p class='label'><%= book.author.name %></p>
</div> <!-- grid-item -->